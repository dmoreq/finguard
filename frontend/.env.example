NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Rasa CALM backend (required for chat in production)
RASA_URL=http://localhost:5005

# Optional: headless/API testing only — UI requires Supabase sign-in
ENABLE_DEV_USER_FALLBACK=false
FIN_GUARD_DEV_USER_ID=

# Deprecated — only when ENABLE_LEGACY_AI_PARSE=true (use Rasa instead)
ENABLE_LEGACY_AI_PARSE=false
OPENAI_API_KEY=
AI_MODEL=gpt-4.1-mini

APP_URL=http://localhost:3000
