# Archived Supabase migrations (deferred)

These SQL files are **not used** by the current local-first stack (`backend/data/finguard.db` via aiosqlite).

Keep them for a future phase when Supabase Auth and hosted Postgres return. Apply in filename order to a Supabase project.

See `scripts/apply-migrations.sh` for apply instructions.
